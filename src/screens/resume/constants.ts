import {CSSProperties} from "react";

export const DEFAULT_CONTENT = 'Tablio';

export const selectedContentLinkStyle: CSSProperties = {
    borderColor: 'gray',
    borderWidth: 1,
    borderStyle: 'solid',
    borderBottomWidth: 0
};